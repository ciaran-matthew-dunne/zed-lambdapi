; Syntax overrides for Lambdapi
; These scope names are referenced by the `not_in` lists of the bracket
; pairs in config.toml.

; String scope
(string) @string

; Comment scope (inclusive to handle line comments properly)
(comment) @comment.inclusive
